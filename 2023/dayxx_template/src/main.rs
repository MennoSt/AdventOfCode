use lib::filereader;
use lib::grid::Grid;
use lib::grid::Gridi32;
use lib::utils;

fn main()
{
    let part1 = 0;
    let part2 = 0;
    println!("{} {}", part1, part2);
    assert_eq!((part1, part2), (0,0));
}


#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test1() {
        let test = 0;
        assert_eq!(test, 0);
    }
}